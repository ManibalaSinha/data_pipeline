export default function About() {
  return (
    <div style={{ padding: "2rem", fontFamily: "Arial" }}>
      <h1>About Data Pipeline</h1>
      <p>
        This project processes and transforms datasets using Python pipelines.
        The frontend dashboard is built with Next.js to display jobs and metrics.
      </p>
    </div>
  );
}
